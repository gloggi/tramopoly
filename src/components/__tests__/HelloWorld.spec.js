import { describe, it } from 'vitest'

describe('HelloWorld', () => {
  it('renders properly', () => {
    // TODO use vue testing library for any component tests
    //const wrapper = mount(HelloWorld, { props: { msg: "Hello Vitest" } })
    //expect(wrapper.text()).toContain("Hello Vitest")
  })
})
